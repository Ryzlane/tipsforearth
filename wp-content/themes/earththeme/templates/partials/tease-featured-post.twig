{% extends "partials/tease.twig" %}

{% block content %}
  <div class="tease-featured-post">
	  <h1 class="standard-h1"><a href="{{post.link}}">{{post.title}}</a></h1>
		{% if post.thumbnail.src %}
			<img src="{{post.thumbnail.src}}" />
		{% endif %}
		<p>{{post.preview.length(25)}}</p>
  </div>
{% endblock %}